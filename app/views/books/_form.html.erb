<%= simple_form_for @book, :html => { :multipart => true} do |f| %>
<%= select_tag(:category_id, options_for_select(@categories), :prompt => "Select a category")
<%= f.file_field, :book_img %>
<%= f.input :Title, label: "BookTitle" %>
<%= f.input :Description %>
<%= f.input :Author%>
<%= f.button :submit%>
<% end %>